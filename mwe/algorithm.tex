%%
%% Author: thomas
%% 2/16/19
%%

\documentclass{article}

% Docs of algorithmicx: http://tug.ctan.org/macros/latex/contrib/algorithmicx/algorithmicx.pdf
\usepackage{algorithm} % Has to be loaded even when algorithmicx is included
\usepackage[noend]{algpseudocode} % Loads algorithmicx

\begin{document}

    \begin{algorithm}
        \caption{Euclid’s algorithm}
        \label{euclid}
        \begin{algorithmic}[1] % The number tells where the line numbering should start
            \Procedure{Euclid}{$a,b$} \Comment{The g.c.d.\ of a and b}
                \If{$a<0$ or $b < 0$}
                    \State \Return Invalid parameters!
                \ElsIf{$a,b<0$}
                    \State \Return Also invalid parameters!
                \Else
                    \State $r\gets a \bmod b$
                    \While{$r\not=0$} \Comment{We have the answer if r is 0}
                        \State $a \gets b$
                        \State $b \gets r$
                        \State $r \gets a \bmod b$
                    \EndWhile\label{euclidendwhile}
                    \State \Return $b$\Comment{The gcd is b}
                \EndIf
            \EndProcedure
        \end{algorithmic}
    \end{algorithm}

\end{document}